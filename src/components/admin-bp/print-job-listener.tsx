
'use client';
// This component is disabled because it relies on a Firebase Realtime Database connection,
// which has been removed from the project.
export function PrintJobListener() {
  return null;
}
